<!-- Do not edit this file. It is automatically generated by API Documenter. -->

[Home](./index.md) &gt; [sh-ast-analyze](./sh-ast-analyze.md) &gt; [ShAnalyzeMaxDepthError](./sh-ast-analyze.shanalyzemaxdeptherror.md)

## ShAnalyzeMaxDepthError class

Thrown by `sh-ast/analyze`<!-- -->'s `enumerateCommands` when a tree's genuinely nested structure — subshells within subshells, chained command/process substitutions, deeply nested `if`<!-- -->/`case`<!-- -->/loop/function/`time`<!-- -->/`{ }` bodies, deeply chained `elif` — exceeds its defensive recursion-depth guard. \*\*Not\*\* thrown for a long \*linear\* chain (`|`<!-- -->/`|&`<!-- -->/`&&`<!-- -->/`||` of any realistic length): `enumerateCommands` traverses those iteratively, so chain length alone never grows this guard's depth counter — only genuine tree nesting does.

`enumerateCommands` deliberately fails closed here rather than returning a truncated, partial result: a partial `CommandSite[]` silently omits real command sites, which is a false negative for a permission-hook-style consumer that treats "command not found in the enumeration" as "nothing to worry about" — an explicit, documented throw is safer than a silent under-report. Gate-style consumers should treat this error as `deny`<!-- -->, not fall back to "no commands found, so allow".

**Signature:**

```typescript
export declare class ShAnalyzeMaxDepthError extends ShAstError 
```
**Extends:** [ShAstError](./sh-ast-analyze.shasterror.md)

## Constructors

<table><thead><tr><th>

Constructor


</th><th>

Modifiers


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

[(constructor)(maxDepth)](./sh-ast-analyze.shanalyzemaxdeptherror._constructor_.md)


</td><td>


</td><td>

Constructs a new instance of the `ShAnalyzeMaxDepthError` class


</td></tr>
</tbody></table>

## Properties

<table><thead><tr><th>

Property


</th><th>

Modifiers


</th><th>

Type


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

[code](./sh-ast-analyze.shanalyzemaxdeptherror.code.md)


</td><td>

`readonly`


</td><td>

(not declared)


</td><td>


</td></tr>
<tr><td>

[maxDepth](./sh-ast-analyze.shanalyzemaxdeptherror.maxdepth.md)


</td><td>

`readonly`


</td><td>

number


</td><td>

The maximum nesting depth `enumerateCommands` supports; the same value every time (not caller-configurable).


</td></tr>
</tbody></table>

